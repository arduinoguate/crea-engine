= simple_form_for(@crea_module) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :enabled
    = f.input :module_type
    = f.input :status

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
